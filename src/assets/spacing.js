const baseline = 8;

const spacing = {
  "x-small": `${baseline}px`,
  small: `${baseline * 2}px`,
  medium: `${baseline * 3}px`,
  large: `${baseline * 4}px`,
  "x-large": `${baseline * 5}px`,
};

export default spacing;
